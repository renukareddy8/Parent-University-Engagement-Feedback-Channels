# Optional — if you want the LLM-backed agent, add your OpenAI API key here
OPENAI_API_KEY=
PORT=8000

# Optional SMTP settings to enable email notifications
# If these are not set, notifications will be simulated (printed to stdout)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_TLS=true
